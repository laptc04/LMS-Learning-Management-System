// Define types for props
type PaginationProps = {
  limit: number; //Number of rows per page
  activation: number; //currently selected
  max: number; // total page
};

export default PaginationProps;
